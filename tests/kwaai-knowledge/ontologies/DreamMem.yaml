# Ontology for the DreamMem KB.
# Arm B, foundation-model authored 2026-08-25.
#
# BLOCKED ON INGESTION — do not apply yet.
# See results/corpus_hygiene_20260825_*.md. Applying an ontology to a corpus
# this dirty measures the dirt, not the ontology.
# Corpus hygiene: 74% clean — 569 bibliography chunks.

ontology:
  name: dreammem
  version: 1
  extends: scholarly

provenance:
  derivation: unverified
  note: >-
    thin extension of scholarly; corpus not sampled

entity_types:
  - name: BrainRegion
    description: A named anatomical structure.
    irreducible: false
  - name: SleepStage
    description: A named stage of sleep (NREM, REM, slow-wave).
    irreducible: false
relation_types:
  - name: consolidates_during
    domain: [Phenomenon]
    range:  [SleepStage]
