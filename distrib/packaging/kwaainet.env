# Environment for the kwaainet systemd user unit.
# Installed from a package: `kwaainet update` would fight the package manager,
# so self-update is off. Use apt/dnf to upgrade.
KWAAINET_NO_AUTO_UPDATE=1

# Increase node verbosity, e.g. RUST_LOG=kwaainet=debug
#RUST_LOG=info
